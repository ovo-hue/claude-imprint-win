# Core memory system
imprint-memory @ git+https://github.com/Qizhan7/imprint-memory.git

# HTTP mode (imprint-memory --http) + Dashboard
uvicorn>=0.34.0
starlette>=0.27.0
fastapi>=0.115.0

# Dashboard (scheduled task YAML parsing)
pyyaml>=6.0

# System status (imprint-utils)
psutil>=6.0.0

# Cron daemon (scripts/cron_daemon.py) - no-admin scheduler alternative
schedule>=1.2.0

# Verify TLS against the OS cert store (works behind TLS-intercepting proxies)
truststore>=0.9.0
